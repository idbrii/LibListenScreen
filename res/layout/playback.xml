<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:id="@+id/playback_controls"
>
<!--
  Anchor the Playpause button to the SeekBar. Anchor the other buttons to
  playpause. Anchor the text with the outside buttons.

  Anchor the title to the top and the description below the title.
-->
    <SeekBar
        android:id="@+id/progress"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:layout_alignParentBottom="true"
    ></SeekBar>
    <ImageButton
        android:id="@+id/playpause"
        android:src="@drawable/media_playback_pause"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:layout_above="@id/progress"
        android:layout_centerInParent="true"
    ></ImageButton>
    <ImageButton
        android:id="@+id/rew"
        android:src="@drawable/media_skip_backward"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:layout_above="@id/progress"
        android:layout_toLeftOf="@id/playpause"
    ></ImageButton>
    <ImageButton
        android:id="@+id/ffwd"
        android:src="@drawable/media_skip_forward"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:layout_above="@id/progress"
        android:layout_toRightOf="@id/playpause"
    ></ImageButton>
    <TextView
        android:id="@+id/elapsed"
        android:text="--:--"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:padding="10dp"
        android:layout_above="@id/progress"
        android:layout_alignParentLeft="true"
    ></TextView>
    <TextView
        android:id="@+id/duration"
        android:text="--:--"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:padding="10dp"
        android:layout_above="@id/progress"
        android:layout_alignParentRight="true"
    ></TextView>
    <TextView
        android:id="@+id/title"
        android:layout_height="wrap_content"
        android:layout_width="fill_parent"
        android:text="#title"
        android:textSize="25dp"
        android:layout_margin="10dp"
        android:layout_alignParentTop="true"
    ></TextView>
    <TextView
        android:id="@+id/description"
        android:layout_height="wrap_content"
        android:layout_width="fill_parent"
        android:layout_below="@id/title"
        android:text="#description"
        android:layout_margin="10dp"
    ></TextView>
</RelativeLayout>

